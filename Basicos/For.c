#include <stdio.h>

int main(){

    int i;
    
    for (i = 1; i <= 10; i++){
        // Código que será executado enquanto a condição for verdadeira
        
        printf("%d ", i);
    }
        return 0;
}

// Exemplo de loop for que imprime números de 1 a 10
// Nesse loop, sabemos quando será seu fim, pois i começa em 1 e vai até 10, incrementando de 1 em 1.
// O loop for é útil quando sabemos o número exato de iterações que precisamos fazer.